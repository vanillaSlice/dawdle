branches:
  only:
    - master
sudo: required
services:
  - docker
script:
  - ./scripts/docker-build-and-test.sh
deploy:
  provider: script
  script: ./scripts/docker-build-and-deploy.sh
  on:
    branch: master
after_success:
  - coveralls
